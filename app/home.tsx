import { Text, View } from "react-native";

export default function HomeScreen() {
    return (
        <View className="flex flex-1 items-center justify-center bg-primary">
            <Text className="text-white text-5xl font-bold">Tela principal</Text>

        </View>
    )
}